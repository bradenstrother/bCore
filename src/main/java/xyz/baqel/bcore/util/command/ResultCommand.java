package xyz.baqel.bcore.util.command;

public enum ResultCommand {
    SUCCESS,
    INVALID_USAGE,
    INVALID_PERMISSION,
    INVALID_PLAYER,
    INVALID_SUBCOMMAND,
    INVALID_ARGUMENT,
    CONSOLE_ONLY,
    CRITICAL_ERROR
}
